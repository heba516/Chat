import { VerificationCode } from "@/components/VerificationCode";
import React from "react";

const page = () => {
  return (
    <div>
      <VerificationCode />
    </div>
  );
};

export default page;
